# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.14.0
# psycopg2-binary removed: app uses asyncpg exclusively (postgresql+asyncpg:// driver)

# Auth / Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2          # passlib 1.7.4 detect_wrap_bug() passes >72-byte passwords to hashpw(); bcrypt 4.x raises ValueError on this, bcrypt 3.x silently truncates (expected behaviour)
cryptography==43.0.3

# Settings
pydantic==2.10.3
pydantic-settings==2.6.1
pydantic[email]==2.10.3

# AWS
boto3==1.35.74
botocore==1.35.74

# HTTP
httpx==0.28.0

# ML
numpy==1.26.4
scikit-learn==1.5.2
joblib==1.4.2

# PDF generation
reportlab==4.2.5

# Email
# (smtplib is stdlib)

# Observability
structlog==26.1.0
