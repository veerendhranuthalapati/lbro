# ⚠️  DEPRECATED — DO NOT USE THIS FILE ⚠️
#
# This file is an obsolete development compose fragment with incorrect
# environment variable names, hardcoded ap-south-1 region, and wrong
# build contexts. It will break your stack if used.
#
# Use the authoritative compose files at the repo root:
#
#   Development:  docker compose -f docker-compose.yml up --build
#   Production:   docker compose -f docker-compose.prod.yml up -d
#
# This file intentionally has no services so it cannot be accidentally deployed.
services: {}
