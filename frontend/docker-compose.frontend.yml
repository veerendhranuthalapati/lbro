# ⚠️  DEPRECATED — DO NOT USE THIS FILE ⚠️
#
# This fragment references a non-existent service 'lbro-backend' and
# exposes port 5173 which the production Nginx image does not serve.
#
# The frontend service is defined in the root docker-compose.yml (dev)
# and docker-compose.prod.yml (production). Use those instead.
services: {}
