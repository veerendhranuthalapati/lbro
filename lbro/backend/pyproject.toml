[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
testpaths = ["tests"]
addopts = "-v --tb=short --no-cov"
filterwarnings = ["ignore::DeprecationWarning", "ignore::RuntimeWarning"]

[tool.coverage.run]
omit = ["tests/*", "app/migrations/*"]
